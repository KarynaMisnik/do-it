<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- DB connection -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>

        <property name="hibernate.connection.url">
            jdbc:mysql://localhost:3306/todo?useSSL=false&amp;allowPublicKeyRetrieval=true
        </property>

        <property name="hibernate.connection.username">todo_user</property>
        <property name="hibernate.connection.password">Todo1234!</property>

        <!-- Hibernate -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <property name="hibernate.hbm2ddl.auto">update</property>
        <property name="hibernate.show_sql">true</property>

        

        <!-- Entities -->
        <mapping class="todo.User"/>
        <mapping class="todo.Task"/>
        <mapping class="todo.Category"/>
        <mapping class="todo.Comment"/>
        <mapping class="todo.Priority"/>

    </session-factory>
</hibernate-configuration>